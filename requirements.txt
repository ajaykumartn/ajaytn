# FastAPI and dependencies
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12
aiofiles==24.1.0
jinja2==3.1.4
python-dotenv==1.0.1

# Keep-alive service
requests==2.32.3

# Email
# (uses built-in smtplib)

# Session management
itsdangerous==2.2.0

# Pydantic for data validation (newer versions with pre-built wheels)
pydantic==2.9.2
pydantic-settings==2.5.2
